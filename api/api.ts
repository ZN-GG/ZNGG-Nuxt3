import { article } from '~/api/article';
import { user } from '~/api/user';
import { upload } from '~/api/upload';
import { fun } from '~~/api/fun';
import { tool } from '~~/api/tool';

export const api = {
    article, user, upload, fun, tool
} 